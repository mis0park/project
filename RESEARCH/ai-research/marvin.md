
# object detection
bounding box (bbox)
(cx,cy)
find optic disk
detection network 
-> YOLO Network(You Only Look Once) v5 v7

ROI (Region of Interest (medical term))
= optic disk
transfer learning = learning with knowledge (on top, not from scratch)
transfer brain?
